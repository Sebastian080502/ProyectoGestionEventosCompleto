import { montserrat } from "@/app/fonts";


export default function HomePage() {
  return (
    <section>
      <h2 className={montserrat.className}>Bienvenido a la Gestión de Eventos</h2>
      <p>Utiliza el menú lateral para navegar entre las secciones.</p>
    </section>
  );
}
